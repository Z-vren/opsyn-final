1. When a new row is added to a Google Sheets “Expenses” sheet, check if the Amount is greater than 1000; if yes, send a high-priority Slack message to #finance and create an Airtable record in “Large Expenses”; otherwise, post a normal Slack update and just append a note back to the same Sheets row.
2. When a new invoice email arrives in Gmail, extract the due date and calculate days overdue; if days overdue is greater than 14, create a Notion task “Escalate invoice” and DM the finance lead in Slack; otherwise, file it into a “To Pay This Week” Notion database and send a polite acknowledgement reply in Gmail.
3. When a new order comes into Shopify, check if the order total is between 50 and 200; if yes, add the customer to a Mailchimp “Standard Buyers” audience and notify #sales in Slack; otherwise, create an Airtable record in “Out-of-band Orders” and ping #ops to review.
4. When a Google Forms quiz response is submitted, check the score: if the score is greater than or equal to 80, email a “Congrats” message via Gmail and add them to an Airtable “High Performers” table; if the score is less than 50, email a support follow-up and add them to “Needs Help”; otherwise, send a standard confirmation email and log them to Google Sheets.
5. When a new bug report is created in Airtable, check if the SeverityScore equals 5; if yes, create a GitHub Issue labeled “P0” and post to Slack #oncall; otherwise, create a normal GitHub issue labeled “triage” and post to #engineering.
6. When a new Intercom conversation is opened, check if the customer plan tier number is greater than 3; if yes, assign it to the “Priority Support” teammate and send a Slack alert to #support-priority; otherwise, assign it to the standard queue and log the ticket to Google Sheets.
7. When a new lead is created in HubSpot, check if the LeadScore is greater than 70; if yes, create a Notion follow-up task due tomorrow and send a Gmail intro email template; otherwise, add the lead to an Airtable “Nurture” base and notify #marketing in Slack.
8. When a new metric row is appended to Google Sheets “Server Health”, check if CPU_UsagePercent is greater than 85; if yes, create a PagerDuty incident and post an urgent message in Slack #infra-alerts; otherwise, just post a brief status update in #infra.
9. When a meeting is created in Google Calendar, check if the duration in minutes is greater than 120; if yes, send a Gmail note to the organizer suggesting an agenda and create a Notion agenda page; otherwise, just create the Notion agenda page without emailing.
10. When a login audit event is received via Webhook, check if the failedAttempts count is greater than 3; if yes, create a Notion security incident and notify Slack #security; otherwise, append the event to Google Sheets “Auth Logs” only.
11. When a new budget update row is added in Airtable, check if PercentUsed is greater than or equal to 90; if yes, DM the budget owner in Slack and create a Notion “Budget Review” task; otherwise, post a weekly summary line to a Google Sheet “Budget Tracker”.
12. When a new inventory update comes in (from Shopify or a Webhook) and is logged to Google Sheets, check if StockCount is less than 10; if yes, create a reorder task in Notion and alert #ops in Slack; otherwise, just mark the row as “OK” in Sheets.
13. When a new temperature reading is pushed to Airtable “Sensors”, check if TemperatureC is greater than 40; if yes, send a Slack message to #facilities and email maintenance via Gmail; otherwise, just store the reading and do nothing else.
14. When a task is created in Notion “Backlog”, check if EstimateHours is greater than 8; if yes, create a GitHub Issue labeled “large-task” and notify the tech lead in Slack; otherwise, create the GitHub issue labeled “small-task” and post to #dev-updates.
15. When an expense reimbursement request arrives via Google Forms, check if the ReimbursementAmount equals 0; if yes, email the submitter via Gmail asking for a valid amount and mark the row as “Invalid” in Google Sheets; otherwise, create an approval item in Airtable and notify #finance in Slack.
16. When a candidate record is created in Airtable “Applicants”, check if YearsExperience is greater than or equal to 5; if yes, send a Gmail interview invite and create a Google Calendar event; otherwise, send a polite “we’ll keep you in mind” email and tag the Airtable record as “junior”.
17. When a new feedback form response is saved to Google Sheets, check the NPS rating: if it is less than or equal to 6, create a Notion follow-up task and alert Slack #customer-success; otherwise, log it to an “All Feedback” Airtable table and send a thank-you email via Gmail.
18. When a new event is created in Google Calendar, check if AttendeeCount is greater than 15; if yes, auto-create a Google Meet link and post joining instructions to Slack #all-hands; otherwise, send details only to attendees via Gmail.
19. When a shipping request row is added to Airtable, check if PackageWeightKg is greater than 20; if yes, notify #logistics in Slack and create a “Freight Required” task in Notion; otherwise, mark it “Standard Shipping” and email the customer via Gmail.
20. When a new transaction is recorded in Google Sheets, check if Amount is between 100 and 300; if yes, categorize it automatically in Airtable “Medium Transactions”; otherwise, if Amount is greater than 300, categorize as “Large”, and if Amount is less than 100, categorize as “Small” and post a short Slack summary.
21. When a new API performance log arrives via Webhook, check if ResponseTimeMs is greater than 2000; if yes, create a GitHub Issue titled “API slow: >2000ms” and ping Slack #backend; otherwise, append the stats to Google Sheets “API Metrics”.
22. When a task due date is updated in Notion, calculate days remaining; if days remaining is less than or equal to 2, send a reminder in Slack to the assignee and email them via Gmail; otherwise, just update a Google Sheets “Task Timeline” row.
23. When a document is submitted via Google Forms (text answer) and stored in Notion, check if the word count is greater than 500; if yes, assign it to the reviewer in Notion and post in Slack #reviews; otherwise, send it back via Gmail asking for more detail and label it “Too Short” in Notion.
24. When a daily GitHub summary job runs, check if the OpenPRCount is greater than 10; if yes, post a “PR backlog” alert in Slack #dev and create a Notion task “PR Cleanup”; otherwise, post a normal status update to Slack only.
25. When a new web audit row is added to Google Sheets, check if the PerformanceScore is less than 60; if yes, open a GitHub Issue with the audit details and notify Slack #web; otherwise, store the audit and mark the row “Pass”.
26. When a payment webhook event is received, check if RetryCount equals 2; if yes, send a Gmail warning email to the customer and create an Airtable record “At Risk Payment”; otherwise, if RetryCount equals 0, send a standard receipt email and log to Google Sheets.
27. When a sustainability form response is stored in Airtable, check if CO2Kg is greater than 100; if yes, create a Notion action item “Reduce CO2 > 100kg” and notify Slack #ops; otherwise, just add it to a monthly summary sheet in Google Sheets.
28. When a discount request row is added to Google Sheets, check if DiscountPercent is greater than or equal to 30; if yes, require approval by creating a Notion approval task and notifying Slack #sales-leads; otherwise, auto-approve by updating the sheet status to “Approved” and emailing the requester via Gmail.
29. When a stock alert webhook arrives and you log it into Airtable, check if PriceChangePercent is greater than 5; if yes, send an urgent message to Slack #trading and email the analyst via Gmail; otherwise, just keep it in Airtable for the daily digest.
30. When a weekly timesheet row is added to Google Sheets, check if TotalHours is less than 35; if yes, send a check-in message to the employee in Slack and create a Notion note for the manager; otherwise, mark the timesheet row as “OK” and do nothing else.
31. When a new email arrives in Gmail, check if the subject contains "Invoice #"; if yes, save the details to Airtable “Invoices” and notify #finance in Slack; otherwise, if the subject contains “Receipt”, store it in a Notion “Receipts” database, and otherwise label it “General” in Gmail.
32. When a message is posted in Slack #support, check if the message starts with "BUG:"; if yes, create a GitHub Issue and reply in the thread with the issue link; otherwise, create a Notion support note and add a row to Google Sheets “Support Log”.
33. When a new GitHub issue is created, check if the title contains "[SECURITY]"; if yes, post to Slack #security and assign the security lead; otherwise, post to #engineering and assign the normal triage rotation.
34. When a new page is created in Notion, check if the Category equals "Blog"; if yes, create a Google Docs draft link and send it to the editor via Gmail; otherwise, if Category equals “Spec”, create a GitHub Issue for implementation, and otherwise just post a note to Slack.
35. When a new record is created in Airtable “Leads”, check if the Country equals "Pakistan"; if yes, route it to the “PK Sales” owner and send a Slack message to #sales-pk; otherwise, route it to the “Global Sales” owner and post in #sales.
36. When an email comes in via Gmail, check if the from address contains "@vipclient.com"; if yes, immediately post it to Slack #vip-support and create a high-priority task in Notion; otherwise, add it to an Airtable “Inbox Triage” table for later.
37. When a new row is added to Google Sheets “Orders”, check if the Status equals "Cancelled"; if yes, send a cancellation acknowledgement via Gmail and notify #ops in Slack; otherwise, if Status equals “Paid”, notify #shipping, and otherwise just log it.
38. When a new ticket is created in Airtable, check if the Notes field is empty; if yes, email the requester via Gmail asking for details and set Airtable status to “Waiting for Info”; otherwise, post the ticket summary to Slack #support and create a Notion task.
39. When a message is posted in Slack, check if the channel name equals "helpdesk"; if yes, create a record in Airtable “Helpdesk Tickets”; otherwise, if the channel name equals “announcements”, mirror the message into a Notion “Company Updates” page, and otherwise do nothing.
40. When code is pushed to GitHub, check if the branch name starts with "hotfix/"; if yes, post an urgent message to Slack #release and create a Notion change log entry; otherwise, post a normal update in #dev-updates.
41. When a new Google Forms response arrives, check if the free-text answer contains "refund"; if yes, create an Airtable case “Refund Request” and notify #billing in Slack; otherwise, if it contains “bug”, create a GitHub Issue, and otherwise store it in Notion as general feedback.
42. When a new issue is created in Jira (or logged similarly in Notion), check if the summary contains "login"; if yes, notify Slack #auth-team and create a GitHub tracking issue; otherwise, notify #general-dev and log it to Google Sheets.
43. When an item is added to Notion “Requests”, check if the Priority equals "High"; if yes, post to Slack #urgent and create a GitHub Issue with label “high”; otherwise, post to #requests and just keep it in Notion.
44. When a new email arrives in Gmail, check if the body contains "unsubscribe"; if yes, add the sender to a Google Sheets “Do Not Contact” list and send a confirmation reply; otherwise, route the email to an Airtable “Leads” table for outreach.
45. When a company is added to Airtable “Accounts”, check if the CompanyName starts with "Acme"; if yes, notify Slack #account-managers and create a “Special Handling” page in Notion; otherwise, create a standard Notion account page only.
46. When a new row is added to Google Sheets “Campaigns”, check if the CouponCode is empty; if yes, message Slack #marketing to add a coupon and set status to “Blocked”; otherwise, if CouponCode equals “WELCOME10”, tag it “New Users” in Airtable, and otherwise tag it “Other”.
47. When a GitHub issue is created, check if the label text contains "needs-review"; if yes, post to Slack #code-review and assign the reviewer; otherwise, post to #triage and leave it unassigned.
48. When a message is posted in Slack, check if the message text contains "prod down"; if yes, create a PagerDuty incident and open a Notion incident page; otherwise, if it contains “staging down”, just post to #infra, and otherwise do nothing.
49. When an email arrives in Gmail, check if the subject starts with "Re:"; if yes, attach it to the existing thread record in Airtable “Conversations”; otherwise, create a brand-new Airtable conversation record and post a Slack heads-up.
50. When a page is created in Notion, check if the Title equals "Weekly Report"; if yes, export it (or copy content) into Google Docs and notify Slack #leadership; otherwise, store it only in Notion and log the creation to Google Sheets.
51. When a deal record is updated in Airtable, check the Stage: if Stage equals "Closed Won", send a welcome email via Gmail and post a win message in Slack #sales-wins; if Stage equals "Closed Lost", send a feedback request email and log it to Google Sheets; otherwise, just update the Notion pipeline page.
52. When a new employee request row is added to Google Sheets, check if the Department equals "HR"; if yes, create a task in Notion “HR Queue” and notify #hr in Slack; otherwise, if Department equals “Engineering”, create a GitHub issue for IT setup, and otherwise log it to Airtable “Other Requests”.
53. When a GitHub issue is created, check if the description contains "steps to reproduce"; if yes, move it to “Ready for Dev” in Notion and notify Slack #dev; otherwise, comment on the issue asking for repro steps and log it to Airtable as “Missing Details”.
54. When an inbound email arrives in Gmail, check if the To address equals "support@yourcompany.com"; if yes, create a ticket in Airtable “Support” and send an auto-reply; otherwise, if To equals “jobs@yourcompany.com”, create a Notion applicant entry, and otherwise label it “General”.
55. When a file is added to Google Drive, check if the file name contains ".pdf"; if yes, create a Notion doc entry and post the link in Slack #documents; otherwise, if the file name contains “.csv”, append a row in Google Sheets “Data Drops”, and otherwise do nothing.
56. When a new Google Forms response is submitted, check if the AgreeToTerms checkbox is true; if true, add the user to Airtable “Approved Signups” and send a welcome email via Gmail; otherwise, send an email asking them to accept the terms and do not add them to Airtable.
57. When a GitHub Pull Request is updated, check if merged is true; if true, post a celebration message in Slack #releases and create a Notion release note entry; otherwise, post a reminder in #code-review and update the Notion entry as “Pending”.
58. When a payment record is updated in Airtable, check if Paid is false; if false, send a reminder email via Gmail and notify #billing in Slack; otherwise, mark it “Complete” in Airtable and log it to Google Sheets “Paid Invoices”.
59. When a message is posted in Slack #approvals, check if a :+1: reaction exists within 30 minutes; if it exists, update the related request in Notion to “Approved” and notify the requester via Gmail; otherwise, after 30 minutes mark it “Pending Approval” in Notion and ping the approvers again in Slack.
60. When an email arrives in Gmail, check if hasAttachment is true; if true, save attachments to Google Drive and create a linked record in Airtable “Attachments”; otherwise, just log the email metadata to Google Sheets.
61. When a record is created in Notion “Purchase Requests”, check if Approved is true; if true, send the request to #procurement in Slack and create an order entry in Airtable; otherwise, email the requester via Gmail that it’s not approved yet and keep it in Notion as “Draft”.
62. When a Google Calendar event is created, check if allDay is true; if true, post a short heads-up in Slack #calendar and skip email; otherwise, email the attendees via Gmail with the agenda link and create a Notion agenda page.
63. When a GitHub issue is created, check if locked is false; if false, auto-comment asking for required details and add it to Notion triage; otherwise, if locked is true, notify Slack #moderation and do not comment.
64. When a file is updated in Google Drive, check if starred is true; if true, post the file link to Slack #important-files and create a Notion reference entry; otherwise, just keep a simple audit row in Google Sheets.
65. When a contact is updated in a CRM table in Airtable, check if DoNotContact is true; if true, add their email to a Google Sheets suppression list and stop any Mailchimp syncing; otherwise, sync them to Mailchimp and send a short intro email via Gmail.
66. When a new GitHub issue is created, check the labels array: if it contains "bug", post to Slack #bugs and create a Notion triage task; if it contains "feature", post to #product and create an Airtable feature record; otherwise, post to #triage and log it to Google Sheets.
67. When a Google Forms response includes a multi-select field “Topics”, check the selected options length: if the length is greater than 3, create a Notion task “High complexity request” and notify Slack #intake; otherwise, create a normal Airtable intake record and send a standard Gmail confirmation.
68. When a message is posted in Slack #alerts, check the mentions list: if it contains "@oncall", create a PagerDuty incident and open a Notion incident page; otherwise, just log the alert to Google Sheets and post a low-priority thread reply.
69. When a Google Calendar event is created, check if the attendees list is empty; if it’s empty, email the organizer via Gmail asking them to add attendees and create a Notion checklist; otherwise, create a Slack group chat for attendees and post the meeting link.
70. When a new record is created in Airtable “Projects”, check the linkedTasks list length: if the length equals 0, create three starter tasks in Notion and notify Slack #pm; otherwise, if the length is greater than 0, just sync the existing tasks into a Google Sheets “Project Tracker” row and post a quick Slack update.