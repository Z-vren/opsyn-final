---
title: "GCP"
description: ""
---

This documentation is to deploy OpSyn on VM Instance or VM Instance Group, we should first create VM template

## Create VM Template

First choose machine type (e.g e2-medium)

After configuring the VM Template, you can proceed to click on "Deploy Container" and specify the following container-specific settings:

- Image: activepieces/activepieces
- Run as a privileged container: true
- Environment Variables:
  - `AP_REDIS_TYPE`: MEMORY
  - `AP_DB_TYPE`: SQLITE3
  - `AP_FRONTEND_URL`: http://localhost:80
  - `AP_EXECUTION_MODE`: SANDBOX_PROCESS
  - **Pieces (required for integrations to show):** Do **not** set `AP_PIECES_SOURCE=FILE` or `AP_PIECES_SYNC_MODE=NONE` when using the pre-built image. Use defaults (pieces from database with auto-sync), or explicitly set:
    - `AP_PIECES_SOURCE`: `DB` (default)
    - `AP_PIECES_SYNC_MODE`: `OFFICIAL_AUTO` (default)
- Firewall: Allow HTTP traffic (for testing purposes only)

Once these details are entered, click on the "Deploy" button and patiently wait for the container deployment process to complete.\

After a successful deployment, you can access the ActivePieces application by visiting the external IP address of the VM on GCP.

## Production Deployment

Please visit [ActivePieces](/install/configuration/environment-variables) for more details on how to customize the application.

## Pieces not showing on GCP

If integrations (pieces) do not appear in the flow builder after deploying to **Google Cloud** (VM, Cloud Run, or GKE), set these in your deployment environment:

| Scenario | What to set |
|----------|-------------|
| **Pre-built image** (`activepieces/activepieces` or `ghcr.io/activepieces/activepieces`) | Do **not** set `AP_PIECES_SOURCE=FILE` or `AP_PIECES_SYNC_MODE=NONE`. Use defaults: pieces load from the database and sync automatically. Ensure outbound HTTPS to the piece registry (e.g. npm) is allowed. |
| **Custom image / your own .env** | Either use **DB** (recommended): set `AP_PIECES_SOURCE=DB` and `AP_PIECES_SYNC_MODE=OFFICIAL_AUTO` (or leave both unset). Or use **FILE**: set `AP_PIECES_SOURCE=FILE` and `AP_DEV_PIECES` to a comma-separated list of piece names (e.g. `webhook,slack,schedule,http,delay,code`), and ensure your image includes built pieces under `dist/packages/pieces`. |

**Cloud Run / GKE:** Add the variables above as container environment variables or via Secret Manager / workload env. After changing env, redeploy and allow 1–2 minutes for the first piece sync when using `AP_PIECES_SOURCE=DB`.